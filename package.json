{
  "name": "basewar-io",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "BaseWar.io — .io strategy game. Monorepo: shared simulation + client (+ server from Phase 1).",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @basewar/client",
    "build": "npm run build --workspace @basewar/client",
    "test": "node --test packages/sim/test/ apps/client/test/"
  }
}
